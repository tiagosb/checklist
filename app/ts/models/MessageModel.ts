class MessageModel
{
    public hasMessage: boolean;
    public message: string;

    constructor(hasMessage:boolean, message:string)
    {
        this.hasMessage = hasMessage;
        this.message = message;
    }
}